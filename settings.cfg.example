# Copy to settings.cfg (gitignored; must be TUF-signed as settings.cfg.sig).
# Non-secret config only — secrets live in .env* / Vault.
# NETWORK=cluster|public selects ENDPOINTS.<SERVICE>.CLUSTER|PUBLIC → env as <SERVICE>_URL.

SIGSTORE:
  OIDC_REDIRECT_URL:

GOOGLE:
  OAUTH_REDIRECT_URL:

ENDPOINTS:
  CTLOG:
    CLUSTER:
    PUBLIC:
  DB:
    CLUSTER:
    PUBLIC:
  DEFECTDOJO:
    CLUSTER:
    PUBLIC:
  FULCIO:
    CLUSTER:
    PUBLIC:
  GITLAB:
    CLUSTER:
    PUBLIC:
  GRAFANA:
    CLUSTER:
    PUBLIC:
  GROWTHBOOK:
    CLUSTER:
    PUBLIC:
  KEYCLOAK:
    CLUSTER:
    PUBLIC:
  MATOMO:
    CLUSTER:
    PUBLIC:
  OPENREPLAY:
    CLUSTER:
    PUBLIC:
  OPENSEARCH:
    CLUSTER:
    PUBLIC:
  OPENSEARCH_DASHBOARDS:
    CLUSTER:
    PUBLIC:
  OTEL:
    CLUSTER:
    PUBLIC:
  RABBITMQ:
    CLUSTER:
    PUBLIC:
  REDIS:
    CLUSTER:
    PUBLIC:
  REKOR:
    CLUSTER:
    PUBLIC:
  SONAR:
    CLUSTER:
    PUBLIC:
  TUF:
    CLUSTER:
    PUBLIC:
  VAULT:
    CLUSTER:
    PUBLIC:
